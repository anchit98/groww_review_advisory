# Repo-root install: GitHub Actions CI + Render (when build uses this file).
# Includes FastAPI + uvicorn via backend/requirements.txt.
-r backend/requirements.txt

# Phase 3 MCP HTTP client (CI weekly workflow)
httpx>=0.27.0
